const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const fs=require('node:fs');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage({viewport:{width:1440,height:900}});await page.goto('http://localhost:3200/login');await page.getByRole('button',{name:'로그인',exact:true}).waitFor();await page.evaluate(()=>document.fonts.ready);await page.screenshot({path:'docs/evidence/prototype-login.png',fullPage:true});const styles=await page.evaluate(()=>{const selectors=['body','h1','input','#login-password','button[type=submit]'];return selectors.map(selector=>{const el=document.querySelector(selector);if(!el)return {selector,missing:true};const style=getComputedStyle(el),rect=el.getBoundingClientRect();return {selector,fontFamily:style.fontFamily,fontSize:style.fontSize,color:style.color,background:style.backgroundColor,borderRadius:style.borderRadius,width:rect.width,height:rect.height,x:rect.x,y:rect.y};});});fs.writeFileSync('docs/evidence/prototype-styles.json',JSON.stringify(styles,null,2));console.log('Read-only prototype login screenshot and computed styles captured.');await browser.close();})().catch(e=>{console.error(e.message);process.exitCode=1});
